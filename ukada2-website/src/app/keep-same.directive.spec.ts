import { KeepSameDirective } from './keep-same.directive';

describe('KeepSameDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepSameDirective();
    expect(directive).toBeTruthy();
  });
});
