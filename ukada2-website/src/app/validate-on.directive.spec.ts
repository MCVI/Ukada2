import { ValidateOnDirective } from './validate-on.directive';

describe('ValidateOnDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateOnDirective();
    expect(directive).toBeTruthy();
  });
});
